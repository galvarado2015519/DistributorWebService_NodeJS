// 'use strict'

// const ScheduleController = require("../controllers/Schedule.controller");
// const express = require("express");
// const api = express.Router();

// api.get('/', ScheduleController);
// api.post('/', ScheduleController);
// api.put('/:id', ScheduleController);
// api.delete('/:id', ScheduleController);

// module.exports = api;